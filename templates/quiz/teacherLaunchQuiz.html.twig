<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('assetsUser/assetsQuiz/teacherLaunchQuiz.css') }}">
    <script src="{{ asset('assetStudent/vendor/jquery/jquery.min.js') }}"></script>

    <script src="{{ asset('assetStudent/vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>

    <script src="{{ asset('assetStudent/vendor/jquery-easing/jquery.easing.min.js') }}"></script>

    <title>Quiz</title>
</head>
<body>
<div class="container">
    <h1>Quiz : {{ quiz.title }}</h1>
{#    <div id="connectedStudents">#}
{#       Connected Students :#}
{#    </div>#}

    <button id="nextQuestion">next Question</button>
    <div id="question">

    </div>

</div>
</body>

<script>
    quiz=JSON.parse('{{ quiz|json_encode()|raw }}');
    state=0;

    $('#nextQuestion')[0].addEventListener('click',(e)=>{
        if(state<quiz['questions'].length){
            $('#question')[0].innerHTML=quiz['questions'][state]['question'];
            state++;
            console.log('/teacher/quizState/{{ quiz.id }}/'+state);
            $.ajax({

                url:'/teacher/quizState/{{ quiz.id }}/'+state,
                method: 'POST',
                data: {

                    read: 1
                }
            });


        }
        else {
            state++;
            $.ajax({


                url:'/teacher/quizState/{{ quiz.id }}/'+state,
                method: 'POST',
                data: {

                    read: 1
                }
            });
            $('#question')[0].innerHTML='quiz ended';

        }

    });


    const studentJoined = new EventSource('http://localhost:3000/.well-known/mercure?topic=joinedQuiz{{ quiz.id }}');
    const studentQuit = new EventSource('http://localhost:3000/.well-known/mercure?topic=quitQuiz{{ quiz.id }}');

    students={};

    studentJoined.onmessage = event => {


        data=JSON.parse(event.data);
        if(!students[data[0]]){
            students[data[0]]=data[1]+' '+data[2];

            console.log(students);

            for(studentId in students){
                $('#connectedStudents').append('<div id="student'+studentId +'">' +
                    students[studentId]+
                    '</div>');
            };
        }





    }
    studentQuit.onmessage = event => {

        console.log(event.data);
        $('#student'+event.data).remove();




    }
</script>
</html>