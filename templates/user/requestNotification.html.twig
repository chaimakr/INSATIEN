
{% if caller =='ROLE_STUDENT' %}

    [
    {% for request in requests %}
        {% if loop.last %}
            {
            "id":"{{ request.id }}",
            "teacher":"{{ request.classGroup.owner.firstName }} {{  request.classGroup.owner.lastName  }}",
            "classGroup":"{{ request.classGroup.title }}"
            }
        {% else %}
            {
            "id":"{{ request.id }}",
            "teacher":"{{ request.classGroup.owner.firstName }} {{  request.classGroup.owner.lastName  }}",
            "classGroup":"{{ request.classGroup.title }}"
            },
        {% endif %}
    {% endfor %}
    ]

{% elseif  caller =='ROLE_TEACHER' %}
[
{% for request in requests %}
    {% if loop.last %}
        {
        "id":"{{ request.id }}",
        "student":"{{ request.student.firstName }} {{  request.student.lastName  }}",
        "classGroup":"{{ request.classGroup.title }}"
        }
    {% else %}
        {
        "id":"{{ request.id }}",
        "student":"{{ request.student.firstName }} {{  request.student.lastName  }}",
        "classGroup":"{{ request.classGroup.title }}"
        },
    {% endif %}
{% endfor %}
]


{% endif %}